mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Ddocs=disabled \
    -Dintrospection=enabled \
    -Dvapi=true 
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
