mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Djpeg=enabled \
    -Dman-pages=enabled \
    -Dfish-completions=false \
    -Dbash-completions=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
