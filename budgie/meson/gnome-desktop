mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Dsystemd=disabled \
    -Ddesktop_docs=false \
    -Dintrospection=true \
    -Dgtk_doc=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
