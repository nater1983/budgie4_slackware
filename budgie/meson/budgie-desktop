mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS -Wno-pedantic" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
