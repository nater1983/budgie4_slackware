mkdir cmake-build
cd cmake-build
  cmake .. \
    "${BUDGIE_CMAKE_ARGS[@]}" \
    "${BUDGIE_CMAKE_OPT_ARGS[@]}" \
    -DMAGPIE_DEVELOPMENT=ON \
    -DSDDM=ON
make -C ../cmake-build
make -C ../cmake-build install DESTDIR=$PKG
