mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Dgtk_doc=false \
    -Dicon_update=false \
    -Dsendto=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
