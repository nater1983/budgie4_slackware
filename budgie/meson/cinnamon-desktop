mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Dsystemd=disabled \
    -Ddeprecation_warnings=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
