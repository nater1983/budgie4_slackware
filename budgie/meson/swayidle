mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Dlogind=enabled \
    -Dlogind-provider=elogind
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
