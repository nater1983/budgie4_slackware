mkdir build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Dgtk=true \
    -Dlibsysprof=true \
    -Dsysprofd=bundled \
    -Dsystemdunitdir=/usr/lib/systemd/system \
    -Dhelp=true \
    -Dtools=true \
    -Dtests=false \
    -Dexamples=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
