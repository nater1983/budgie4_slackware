mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Degl_device=true \
    -Dsystemd=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
