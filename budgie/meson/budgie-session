mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Dsystemd=false \
    -Dsystemd_session=disable \
    -Dsystemd_journal=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
