mkdir -p build
cd build
  CFLAGS="$SLKCFLAGS" \
  CXXFLAGS="$SLKCFLAGS" \
  meson setup ..\
    "${BUDGIE_BUILD_ARGS[@]}" \
    $BUDGIE_OPT_ARGS \
    -Dvapi=true \
    -Dlua51=false \
    -Dtests=false \
    -Dgtk_doc=false
  "${NINJA:=ninja}"
  DESTDIR=$PKG $NINJA install
cd ..
