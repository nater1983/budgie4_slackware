# correct desktop entry
sed -i  's/Comment=Budgie Background Services/Comment=budgie-desktop Background Services/' \
	src/daemon/budgie-daemon.desktop.in;
rm -fr subprojects/*
cp -av $CWD/pre-install/budgie-desktop/gvc.wrap subprojects/
